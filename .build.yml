image: archlinux
secrets:
- da1f93b4-c155-406b-9efd-75d9c8d41454f
packages:
  - go
sources:
  - https://github.com/go-ap/auth
environment:
  GO111MODULE: 'on'
tasks:
  - setup: |
      cd auth && go mod download
  - tests: |
      cd auth && make test
  - coverage: |
      set -a +x
      cd auth && make coverage
      GIT_SHA=$(git rev-parse --verify HEAD)
      GIT_BRANCH=$(git name-rev --name-only HEAD)
      source ~/.code-cov.sh
      curl -X POST \
          --data-binary @auth.coverprofile \
           -H 'Accept: application/json' \
          "https://codecov.io/upload/v2?commit=${GIT_SHA}&token=${AUTH_TOKEN}&branch=${GIT_BRANCH}&service=custom"
